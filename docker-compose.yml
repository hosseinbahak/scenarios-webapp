version: "3.9"
services:
  api:
    build:
      context: .
      target: dev     # use "prod" for production
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app  # live-reload during dev
    environment:
      - UVICORN_WORKERS=1
